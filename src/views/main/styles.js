import {StyleSheet} from 'react-native';
export default StyleSheet.create({
  selectedText: {
    fontSize: 20,
  },
  block_counter: {
    position: 'absolute',
    bottom: 0,
    alignItems: 'flex-end',
    justifyContent: 'center',
    width: '100%',
  },
})
